# v0.2.0-prepilot-local-node

Released 2026-05-06.

Pre-pilot product release for the KYNODE Pediatric Local Node. Hosted public review at [pediatric.kynode.io](https://pediatric.kynode.io/) (synthetic data only; see "Public demo mode" below).

## Added

- Offline FastAPI clinic node under `apps/local-node/`.
- SQLite local encounter storage.
- Bilingual clinic UI with Clinic mode as the default.
- Explicit synthetic walkthrough mode.
- Local assessment flow using the existing alpha packages.
- Weekly aggregate surveillance input.
- Weekly climate context input.
- Aggregate signal generation from weekly input only.
- Privacy-bounded export endpoint for zone-level JSON.
- Local audit trail with operational events only.
- AI surveillance brief layer (`POST /api/brief/generate`) with two
  interchangeable generators behind a single schema: a deterministic
  rule-based template (default, always offline) and an opt-in local LLM
  path that talks to Ollama on `localhost:11434` or any Ollama-compatible
  server inside the clinic LAN. No hosted SaaS API is ever called by
  the product code; an explicit operator opt-in
  (`KYNODE_AI_BRIEF_ALLOW_PUBLIC=true`) is required before the call
  layer will dial any non-private endpoint.
- Clinical safety gate post-LLM: if a generated brief contains
  diagnosis, prescription, treatment, dosing or causal-claim phrasing
  (EN or ES patterns), the LLM output is discarded and the
  deterministic generator runs instead. The audit row records which
  generator actually produced the brief.
- Brief generation audit event (`weekly_brief_generated`) with the
  generator name in the `source` column so reviewers can trace at a
  glance whether a brief came from the deterministic template or the
  local LLM path.
- Optional Ollama integration documented at
  `docs/integrations/ollama.{md,es.md}` with install, model selection,
  LAN deployment, troubleshooting and removal steps.

## Safety Boundary

- Patient-level data stays on the device.
- Weekly export is aggregate only.
- Climate notes stay local.
- Audit schema does not store PHI.
- Statistical support only. No autonomous diagnosis.

## Schema Policy

No Alembic migrations are included in this pre-pilot. If an incompatible SQLite schema is detected, the app renames the old file to `*.bak-<timestamp>` and creates a clean v0.2 database.

## Public demo mode

Setting `KYNODE_PUBLIC_DEMO_MODE=true` switches the app into a server-owned synthetic walkthrough:

- The SQLite file is reset on startup.
- All manual writes (`POST /api/assessments`, `POST /api/encounters`, `PUT /api/weekly-inputs`, `PUT /api/climate-context`, `PUT /api/node-settings`) return 403.
- Only the synthetic walkthrough flow (`POST /api/demo/load`, `POST /api/demo/assessment`, `POST /api/demo/encounter`) is callable.
- Aggregate signal and export are scoped to the synthetic zone/week/indicator triple; any other scope returns 403.
- Climate notes are stripped from `GET /api/climate-context` responses.
- A red banner is shown above every page.

This keeps `pediatric.kynode.io` shareable without ever accepting real patient data.

## Verification

```text
184 passed
coverage: 95% (apps/local-node) · 97% (brief.py) · 100% (models)
editable installs: OK
local-node wheel excludes SQLite/DB files: OK
static sensitive-file request returns 404
public demo manual writes return 403
brief endpoint refuses public IP / public DNS by default
brief endpoint writes weekly_brief_generated audit row
clinical safety gate rejects diagnosis / prescription / causal phrasing
  in EN and ES, scans every supported language regardless of the
  requested UI language (small local models occasionally answer in the
  wrong tongue), and falls back to the deterministic generator silently
```

## Still Out Of Scope

- Field validation.
- Production user roles.
- Cloud sync.
- Full IMCI engine.
- CSV climate import.
- Weather API integration.
- Institutional export adapters.
