[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "kynode-pediatric-vaccinations"
version = "0.1.0a0"
description = "Offline vaccination schedule status helpers for KYNODE Pediatric."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "KYNODE" }]

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
kynode_pediatric_vaccinations = ["data/*.json"]
